<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <style>
    /* 默认：白天模式 (白底黑字) */
    :root {
      --bg-color: #ffffff;
      --text-color: #333333;
    }

    @media (prefers-color-scheme: dark) {
      /* 深色模式 (黑底白字) */
      :root {
        --bg-color: #1a1a1a;
        --text-color: #e0e0e0;
      }
    }

    .bg { fill: var(--bg-color); transition: fill 0.3s; }
    .letter { 
      fill: var(--text-color); 
      font-family: '楷体-简-粗体', 'STKaiti', 'KaiTi', '楷体', serif, --apple-system; 
      font-weight: bold;
      transition: fill 0.3s;
    }
  </style>
  
  <rect class="bg" width="100" height="100" rx="20" />
  
  <text x="50%" y="50%" class="letter" 
        font-size="70" text-anchor="middle" dominant-baseline="central">郑</text>
</svg>